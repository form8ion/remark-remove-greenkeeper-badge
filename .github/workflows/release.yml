name: Release
'on':
  push:
    branches:
      - alpha
jobs:
  release:
    uses: form8ion/.github/.github/workflows/release-package.yml@4578dd463821cf5b9a97859db4d774f43bf12130 # master
    secrets:
      NPM_TOKEN: ${{ secrets.NPM_PUBLISH_TOKEN }}
